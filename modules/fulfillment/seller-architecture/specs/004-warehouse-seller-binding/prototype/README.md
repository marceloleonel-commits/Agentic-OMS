# Prototype — Warehouse seller binding

Figma mock: [Warehouse config — Seller binding](https://www.figma.com/design/cavM21wJgGC2qATrE42GE0)

## Current mock coverage

- Warehouse configuration screen with the new **Sellers internos** card.
- Two account states:
  - Conta comum: seller fixed to the main account and disabled.
  - Conta sellerType=3: searchable dropdown to select one account name.

## Pending Figma refinements

The latest visual refinements are pending because the Figma MCP Starter plan hit its tool-call limit:

- Remove the pink highlight and "NOVO" badge from the Seller card.
- Use `lojinhatourinho` as the account name value.
- Remove the lock icon from the disabled field.
