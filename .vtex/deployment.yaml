- name: vertical-distributed-order-management-dom
  acronym: vertical-distributed-order-management-dom
  description: Deploy vertical-distributed-order-management-dom application
  build:
    provider: dkcicd
    pipelines:
    - name: techdocs-v1
      parameters:
        entityReference: default/component/vertical-distributed-order-management-dom
        indexFile: index.md
        sourceDir: ./
      when:
      - event: push
        source: branch
        regex: ^(main)$
        path:
        - "docs/**/*"